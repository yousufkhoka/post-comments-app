import React from 'react';

const CommentImg = (props) => {
    const commentImg = props.image
    console.log(commentImg)
    return (
        <div>
            
        </div>
    );
};

export default CommentImg;